#!/bin/sh

#bind-key MS-i "~/.x/jterm.yawara 80x52+1514+28"

FONT="Hack:size=10"
NAME="st__$(tr -cd '[:alnum:]' < /dev/urandom | fold -w 20 | head -n 1)"


#/usr/local/bin/st -i -f "$FONT" -t $NAME -g $1 $2 $3 &
/usr/local/bin/st -i -f "$FONT" -t $NAME -g $@ &

xdotool search --sync --name $NAME key ctrl+alt+shift+f

